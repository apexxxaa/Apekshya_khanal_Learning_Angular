import { MakeModelPipe } from './make-model.pipe';

describe('MakeModelPipe', () => {
  it('create an instance', () => {
    const pipe = new MakeModelPipe();
    expect(pipe).toBeTruthy();
  });
});
